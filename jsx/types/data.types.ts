// TODO: ELIMINATE THIS EVENUTALLY BY MOVING THINGS TO WHERE THEY SHOULD BE 
import { Specimen, Container, Pool } from '../types';

export type Data = {
  specimens: Specimen[],
  containers: Container[],
  pools: Pool[]
}
